Kohana Database Firebird 0.2
============================

This module is an adaptation of MySql driver provided by the kohana database.

It is still in active development, but there are some tests covering the most
important features for a database driver for kohana. However, it is not
a stable release yet.
